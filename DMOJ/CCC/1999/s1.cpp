// CCC '99 S1 - Card Game
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    
    uint8_t pa = 0, pb = 0;
    for (uint8_t i = 0; i < 52; i++) {
        char cc[5]; cin >> cc;
        if (i & 1) { // player B
            
        } else { // player A

        }
    }

    return 0;
}
